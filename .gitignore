# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment
.env
.env.*

# Scraper cache — regenerated by `npm run fetch-ras`
/data/.ras-cache/

# NOTE: data/rasScores.json IS committed (as an empty {} on fresh checkout)
# so the Vite build doesn't fail on clone. The scraper overwrites it in place.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
